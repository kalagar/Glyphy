{
  "name": "glyphy",
  "private": true,
  "type": "module",
  "description": "Build tooling for the Glyphy browser extension",
  "scripts": {
    "build": "node build.js",
    "package": "bash scripts/build.sh",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
